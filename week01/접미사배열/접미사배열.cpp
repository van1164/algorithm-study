#include<iostream>
#include<string>
using namespace std;

int main() {
	string s;
	cin >> s;
	string s2[1000];
	for (int i = 0; i < s.length(); i++) {
		s2[i] = s.substr(i, s.length() - 1);
	}

	for(int i=0; s2[i+1].)

}